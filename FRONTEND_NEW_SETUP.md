# Новый фронтенд - Инструкция по использованию

## ✅ Что было сделано

Новый фронтенд (`frontend_new/`) теперь **полностью работает с бэкендом**! 

### Основные изменения:
1. ✅ Добавлена инициализация сессии через `/api/get-session`
2. ✅ Добавлено `credentials: 'include'` для работы с cookies
3. ✅ Добавлены функции для работы с `/api/newTasks` и `/api/tasks`
4. ✅ Фронтенд автоматически инициализирует сессию при запуске

## 🚀 Как запустить

### Вариант 1: Использование уже собранного фронтенда

Собранная версия уже находится в `frontend_new/`. Просто запустите бэкенд, и фронтенд будет доступен через FastAPI:

```bash
# Запустите бэкенд (если еще не запущен)
python run.py

# Откройте в браузере:
http://localhost:8000/index.html
# или
http://localhost:8000/
```

### Вариант 2: Разработка и пересборка

Исходный код нового фронтенда находится в `/Users/valerijakovlev/Downloads/scan/`

1. **Создайте файл `.env`** в `/Users/valerijakovlev/Downloads/scan/`:
```bash
VITE_API_URL=http://localhost:8000
VITE_USE_MOCK=false
```

2. **Запустите в режиме разработки:**
```bash
cd /Users/valerijakovlev/Downloads/scan
npm run dev
```

3. **Или соберите для продакшена:**
```bash
cd /Users/valerijakovlev/Downloads/scan
node node_modules/vite/bin/vite.js build
```

4. **Скопируйте собранные файлы:**
```bash
rsync -av --delete --exclude='debug.html' /Users/valerijakovlev/Downloads/scan/dist/ /Users/valerijakovlev/VSCode/LCT_tree_task/frontend_new/
```

## 📋 Используемые API endpoints

Новый фронтенд использует те же endpoints, что и старый:

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/get-session` | GET | Инициализация сессии (автоматически) |
| `/api/analyze` | POST | Анализ изображения |
| `/api/newTasks` | POST | Загрузка нескольких изображений |
| `/api/tasks` | GET | Получение списка задач |
| `/health` | GET | Проверка работы API |

## 🔍 Проверка работы

### 1. Откройте консоль браузера (F12)

Вы должны увидеть:
```
🌐 Инициализация сессии с бэкендом
✅ Сессия инициализирована: { user_id: 1, cookie_token: "..." }
```

### 2. Проверьте cookies

DevTools → Application → Cookies → должна быть cookie `tree_analysis_session`

### 3. Загрузите изображение

Фронтенд автоматически отправит его на анализ через `/api/analyze`

## 🆚 Сравнение со старым фронтендом

| Функция | Старый фронтенд | Новый фронтенд |
|---------|-----------------|----------------|
| Инициализация сессии | ✅ | ✅ |
| Credentials в запросах | ✅ | ✅ |
| Работа с `/api/analyze` | ✅ | ✅ |
| Работа с `/api/newTasks` | ✅ | ✅ |
| Работа с `/api/tasks` | ✅ | ✅ |
| Современный UI | ❌ | ✅ |
| React + TypeScript | ❌ | ✅ |

## 📝 Технические детали

### Исходный код
- Находится в: `/Users/valerijakovlev/Downloads/scan/`
- Технологии: React + TypeScript + Vite + Tailwind CSS
- Основные файлы:
  - `src/services/api.ts` - работа с бэкендом API
  - `src/services/index.ts` - переключение mock/production
  - `src/hooks/useDocumentScanner.ts` - инициализация сессии

### Собранная версия
- Находится в: `frontend_new/`
- Готова к использованию
- Не требует дополнительных настроек

## ❓ FAQ

**Q: Почему не работает загрузка изображений?**
A: Проверьте:
1. Запущен ли бэкенд на порту 8000
2. Есть ли cookie `tree_analysis_session` в браузере
3. Нет ли ошибок в консоли браузера

**Q: Как переключиться между mock и реальным API?**
A: Измените `VITE_USE_MOCK` в `.env` файле и пересоберите фронтенд

**Q: Где посмотреть детальную документацию?**
A: Смотрите `/Users/valerijakovlev/Downloads/scan/BACKEND_INTEGRATION.md`

---

**Готово! Новый фронтенд теперь работает с бэкендом 🎉**

